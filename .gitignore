# Scan results (user-specific, never commit)
scan-results/

# Node (node_modules IS committed because gemini extensions install is a file copy — no npm install hook)
# If you add dependencies, commit node_modules/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Legacy Python (safe to remove after migration)
venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.eggs/
